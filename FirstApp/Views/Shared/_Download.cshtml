
<li class="dropdown">
    <a href="#" class="dropdown-toggle" data-toggle="dropdown">Download <span class="caret"></span></a>
    <ul class="dropdown-menu" role="menu">
@{
    DirectoryInfo dir_info = new DirectoryInfo(Server.MapPath("~/Downloads/"));
    foreach (var file_info in dir_info.GetFiles())
    {
        <li> @Html.ActionLink(file_info.Name, "Download", "Files", new { file = file_info.Name }, null) </li>
        <li>  </li>
    }
}
    </ul>
</li>
